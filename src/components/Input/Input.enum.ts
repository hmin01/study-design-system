/************************************
 * 아이콘 크기 유형
 ************************************/
export enum IconSizeVariants {
  sm = 14,
  md = 16,
  lg = 18,
}
/************************************
 * 요소 크기에 따른 좌/우 패딩
 ************************************/
export enum PaddingVariants {
  sm = 12,
  md = 12,
  lg = 16,
}
