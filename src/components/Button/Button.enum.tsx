import { SizeVariants } from "@/shard/enums";

/************************************
 * 버튼 모양 유형
 ************************************/
export enum ButtonShapeVariants {
  default = "default",
  ghost = "ghost",
  outline = "outline",
}
/************************************
 * 아이콘 버튼 크기 유형
 ************************************/
export enum IconButtonSizeVariants {
  xs = 16,
  sm = 18,
  md = 20,
  lg = 20,
  xl = 20,
}
